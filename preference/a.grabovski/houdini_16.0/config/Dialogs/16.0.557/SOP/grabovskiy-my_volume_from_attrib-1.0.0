// Magic: 718a5ffe
// Automatically generated PSI dialog (PSI version 16.0.557)
// Fri Apr  7 18:05:52 2017


f_tmp = FLOAT_FIELD;	// dialog.top.ui

rank.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH

    PARM_LABEL_SIMPLE("Rank", "rank") WIDTH(1.5);
    { WIDTH(0.09) }

    rank.menu;

    { HSTRETCH }

}

name.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    name.label = PARM_LABEL("Field Name", "name") WIDTH(1.5);
    PARM_FIELD(string, "name") VALUE(name.val) 
	LABELREF(name.label) HSTRETCH;

}

divsize.gad = 
{
 LAYOUT(horizontal) HSTRETCH

 divsize.label = PARM_LABEL("Div Size", "divsize") WIDTH(1.5);
 PARM(float, 1, "divsize", none, none) RANGE(0,10)
	LOCK_RANGE_MIN LABELREF(divsize.label) HSTRETCH;

}
pointattribs.row.join =		
{
  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
pointattribs.join =		
{
  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

pointattribs.gad = PARM_BUTTON("pointattribs", "") toggle VALUE(pointattribs.val);

pointattriblist.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    pointattriblist.label = PARM_LABEL("Points", "pointattriblist") WIDTH(1.32);
    PARM_FIELD(string, "pointattriblist") VALUE(pointattriblist.val) 
	LABELREF(pointattriblist.label) HSTRETCH;

    pointattriblist.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

}
}	
}	// Template join.bot

primitiveattribs.row.join =		
{
  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
primitiveattribs.join =		
{
  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

primitiveattribs.gad = PARM_BUTTON("primitiveattribs", "") toggle VALUE(primitiveattribs.val);

primattriblist.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    primattriblist.label = PARM_LABEL("Primitives", "primattriblist") WIDTH(1.32);
    PARM_FIELD(string, "primattriblist") VALUE(primattriblist.val) 
	LABELREF(primattriblist.label) HSTRETCH;

    primattriblist.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

}
}	
}	// Template join.bot


vdb.gad = ROW
{
    HSTRETCH

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("vdb", "Convert to VDB") toggle VALUE(vdb.val);

    { HSTRETCH }

}

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH LOOK(plain) 
    rank.gad; 
    name.gad; 
    divsize.gad; 
    pointattribs.row.join; 
    primitiveattribs.row.join; 
    vdb.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
