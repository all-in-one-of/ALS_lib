// Magic: 5738a140
// Automatically generated PSI dialog (PSI version 16.0.557)
// Fri Apr  7 18:00:27 2017


f_tmp = FLOAT_FIELD;	// dialog.top.ui

centroid.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH

    PARM_LABEL_SIMPLE("Compute Centroid By", "centroid") WIDTH(1.5);
    { WIDTH(0.09) }

    centroid.menu;

    { HSTRETCH }

}

direction.gad = ROW
{
   HSTRETCH

   direction.label = PARM_LABEL("Direction", "direction") WIDTH(1.5);
   PARM(float, 3, "direction", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(direction.label)
	HSTRETCH;

}

transform.gad = ROW
{
    HSTRETCH

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("transform", "Move Geo") toggle VALUE(transform.val);

    { HSTRETCH }

}

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH LOOK(plain) 
    centroid.gad; 
    direction.gad; 
    transform.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
